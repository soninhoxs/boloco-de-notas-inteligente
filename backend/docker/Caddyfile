# Mega Brain — https://megabrain.app (defina SITE_DOMAIN no .env)
{$SITE_DOMAIN} {
	encode gzip

	handle /health {
		reverse_proxy api:8080
	}

	handle /api/* {
		reverse_proxy api:8080
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
