# API — use caminho relativo (proxy Vite em dev, nginx/Caddy em produção)
VITE_API_URL=/api/v1

# Optional: MapLibre tile server
VITE_MAP_STYLE_URL=https://api.maptiler.com/maps/streets/style.json?key=YOUR_KEY
